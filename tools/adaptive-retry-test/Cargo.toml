[package]
name = "adaptive-retry-test"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-sdk-dsql = "1.55.0"
aws-config = { version = "1", features = ["behavior-version-latest"] }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
