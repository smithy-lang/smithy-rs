[package]
name = "aws-sigv4"
version = "0.0.0-smithy-rs-head"
authors = ["AWS Rust SDK Team <aws-sdk-rust@amazon.com>", "David Barsky <me@davidbarsky.com>"]
description = "SigV4 signer for HTTP requests and Event Stream messages."
edition = "2021"
exclude = ["aws-sig-v4-test-suite/*", "aws-sig-v4a-test-suite/*"]
license = "Apache-2.0"
repository = "https://github.com/awslabs/smithy-rs"

[features]
sign-http = ["http", "percent-encoding", "form_urlencoded"]
sign-eventstream = ["aws-smithy-eventstream"]
default = ["sign-http"]
sigv4a = ["p256", "num-bigint", "zeroize", "aws-smithy-types"]

[dependencies]
aws-smithy-eventstream = { path = "../../../rust-runtime/aws-smithy-eventstream", optional = true }
aws-smithy-http = { path = "../../../rust-runtime/aws-smithy-http" }
aws-smithy-runtime-api = { path = "../../../rust-runtime/aws-smithy-runtime-api", features = ["client"] }
aws-smithy-types = { path = "../../../rust-runtime/aws-smithy-types", optional = true }
aws-credential-types = { path = "../aws-credential-types" }
bytes = "1"
form_urlencoded = { version = "1.0", optional = true }
hex = "0.4"
hmac = "0.12"
http = { version = "0.2", optional = true }
num-bigint = { version = "0.4", optional = true }
once_cell = "1.8"
p256 = { version = "0.13.2", features = ["ecdsa"], optional = true }
percent-encoding = { version = "2.1", optional = true }
regex = "1.5"
ring = "0.16"
sha2 = "0.10"
time = "0.3.5"
tracing = "0.1"
zeroize = { version = "^1", optional = true }

[dev-dependencies]
aws-credential-types = { path = "../aws-credential-types", features = ["test-util", "hardcoded-credentials"] }
criterion = "0.4"
hex-literal = "0.4.1"
httparse = "1.8"
libfuzzer-sys = "0.4.6"
pretty_assertions = "1.3"
proptest = "1.2"
serde = "1.0.180"
serde_derive = "1.0.180"
serde_json = "1.0.104"
time = { version = "0.3.5", features = ["parsing"] }

[target.'cfg(not(any(target_arch = "powerpc", target_arch = "powerpc64")))'.dev-dependencies]
ring = "0.16"

[[bench]]
name = "hmac"
harness = false

[[bench]]
name = "sigv4a"
harness = false

[package.metadata.docs.rs]
all-features = true
targets = ["x86_64-unknown-linux-gnu"]
rustdoc-args = ["--cfg", "docsrs"]
# End of docs.rs metadata
